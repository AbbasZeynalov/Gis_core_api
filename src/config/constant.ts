export enum AUTH_ENTITIES {
    PARCEL = 1,
    BUILDING = 2,
    USER = 3
}

export enum AUTH_OPERATIONS {
    CREATE = 1,
    UPDATE = 2,
    DELETE = 3
}

export enum ON_OFF_STATUS {
    OFF = '0',
    ON = '1'
}
